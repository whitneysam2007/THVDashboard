// THV Donor Dashboard — Initial Data
// All arrays start empty. Add donors, trips, and initiatives via the dashboard UI.

import { DashboardStore } from './types';

export const initialData: DashboardStore = {
  donors: [],
  trips: [],
  initiatives: [],
};
